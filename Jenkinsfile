pipeline {
    agent any
    stages {
        stage('build app') {
            steps { 
                echo 'building app'
            }
        }
      stage('test') {
            steps { 
                echo 'testing app'
            }
        }
       stage('deploy') {
            steps { 
                echo 'deploying app'
            }
        }
    }
}
